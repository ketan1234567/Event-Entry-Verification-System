npm init -y
npm install express cors jsonwebtoken bcryptjs body-parser



what is  password here 

CREATE TABLE IF NOT EXISTS admin (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) NOT NULL UNIQUE,
    email VARCHAR(100) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL,
    role ENUM('admin', 'staff', 'user') DEFAULT 'staff',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

correct sentance ?

I am uaable to see this images


JWT_SECRET what should be 


catch (error) {
    console.error('Error:', error);
    
    // Check if the response was not ok and parse text to see HTML errors
    if (error.message.includes('JSON')) {
        alert('Server is not responding correctly. Is Node.js running? (Port 3000)');
    } else {
        alert('Cannot connect to server.');
    }
    
    btn.innerHTML = originalBtnText;
    btn.disabled = false;
}


-- 2. Insert the admin user (using INSERT IGNORE to prevent errors if user already exists)
INSERT IGNORE INTO users (username, email, password, role) 
VALUES (
    'admin', 
    'admin@ondirect.com', 
    '$2a$10$92IXUNpkjO0rOQ5byMi.Ye4oKoEa3Ro9llC/.og/at2.uheWG/igi', 
    'admin'


admin123
);

payload

{email: "admin@ondirect.com", password: "admin123"}
email
: 
"admin@ondirect.com"
password
: 
"admin123"

response

{"success":false,"message":"Invalid email or password"}




this is dashboard side am I right or not ?

res.json({
            success: true,
            message: 'Login successful',
            token: token,
            user: {
                id: user.id,
                username: user.username,
                email: user.email,
                role: user.role
            }
        });
async function verifyTokenAndGetUser(token) {
            try {
                // --- UNCOMMENT THIS FOR YOUR REAL API ---
                
                const response = await fetch('http://localhost:3000/api/public/me', {
                    headers: { 'Authorization': `Bearer ${token}` }
                });
                
                if (!response.ok) throw new Error("Unauthorized");
                
                const data = await response.json();
                
                // Assuming your API returns { user: { role: 'admin', name: '...', email: '...' } }
                // Adjust 'data.user.role' based on your actual API response structure
                CURRENT_USER = { 
                    role: data.user.role, 
                    name: data.user.name, 
                    email: data.user.email 
                };

                // --- END OF REAL API BLOCK ---

                // Step 3: Apply Security Logic
                initializeDashboard();

            } catch (error) {
                console.error("Token Verification Failed:", error);
                // Token invalid or expired -> Logout
                localStorage.removeItem('authToken'); // <--- MATCH KEY HERE TOO
                window.location.href = 'login.html';
            }
        }





here I want add and insert database to this password bcryptjs like all I want


const bcrypt = require('bcryptjs');

 bcrypt.hash(password, 10, (err, hash) => {
   if (err) throw err;
   console.log('--------------------------------');
  console.log('Copy this SQL query and run it in your Database:');
    console.log(`UPDATE admin SET password = '${hash}' WHERE email = 'admin@ondirect.com';`);
     console.log('--------------------------------');
});


I want bcryptjs password

INSERT  INTO admin (username, email, password, role) 
VALUES (
    'get1', 
    'get1@ondirect.com', 
    'get1123', 
    'get1'
);

INSERT  INTO admin (username, email, password, role) 
VALUES (
    'get2', 
    'get2@ondirect.com', 
    'get2123', 
    'get2'
);

INSERT  INTO admin (username, email, password, role) 
VALUES (
    'get3', 
    'get3@ondirect.com', 
    'get3123', 
    'get3'
);


INSERT  INTO admin (username, email, password, role) 
VALUES (
    'get4', 
    'get4@ondirect.com', 
    'get4123', 
    'get4'
);




const bcrypt = require('bcryptjs');

const users = [
    { username: 'get1', email: 'get1@ondirect.com', password: 'get1123', role: 'get1' },
    { username: 'get2', email: 'get2@ondirect.com', password: 'get2123', role: 'get2' },
    { username: 'get3', email: 'get3@ondirect.com', password: 'get3123', role: 'get3' },
    { username: 'get4', email: 'get4@ondirect.com', password: 'get4123', role: 'get4' }
];

console.log('-- COPY THE SQL QUERIES BELOW AND RUN IN PHPMYADMIN --');

users.forEach(user => {
    // Use hashSync to ensure we get the hash immediately and keep order
    const hash = bcrypt.hashSync(user.password, 10);
    
    // added created_at = NOW() to match your table structure
    const sql = `INSERT INTO admin (username, email, password, role, created_at) VALUES ('${user.username}', '${user.email}', '${hash}', '${user.role}', NOW());`;
    
    console.log(sql);
});

console.log('-- END OF SQL --');

Akamai


dbreak


First Touch Asset title - MSM6


Second Touch Asset - G-MSM-0
G-MST-1
G-MSM-2
